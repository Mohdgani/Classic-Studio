# TODO

- [x] Fix Gallery image paths so images render correctly on `/Suit-Maker/`.
- [x] Verify Navbar `to="/process"` has a matching route and confirm behavior with `BrowserRouter basename="/Suit-Maker"`.
- [ ] Build the project to ensure TypeScript/Vite compiles (may already be OK).
- [ ] Re-run `npm run dev` and manually verify:
  - Gallery images show
  - Navbar Process link navigates to the Process section/page as expected
